# Core runtime dependencies (flexible versions)

# Removed Pillow dependency (pillow 11.3.0)
# Reason: GHSA-whj4-6x5x-4v2j (FITS decompression bomb vulnerability)
# Impact: Denial of service via malicious FITS image files
#
# This Project does not process FITS or untrusted image inputs.
# Pillow was previously included but is no longer used by the application.
# It has been removed from project dependencies, and will be fully removed
# from the build environment in v1.9.1 to reduce attack surface
# and eliminate unnecessary dependencies.

PySide6>=6.9
cryptography>=45
requests>=2.32
urllib3>=2.5
psutil>=7.2
pyotp>=2.9
qasync>=0.28
filelock>=3.19

# Optional / hardware / security features
fido2>=2.0
yubikey-manager>=5.8
hidapi>=0.14
pyscard>=2.3
cbor2>=5.9

# Extras used in project
lxml>=6.0
reportlab>=4.4
qrcode>=8.2
opencv-python>=4.12
